function HomeScreen() {
  // const users = useSelector((state) => state.user);
  // const dispatch = useDispatch();

  return (
    <div>
      <h1>Home Screen</h1>
    </div>
  );
}

export default HomeScreen;
